<div
  class='banners-container-slider'
  data-rotation-speed='{{ section.settings.rotation_speed }}'
  data-auto-play='{{ section.settings.auto_play }}'
  data-show-controls='{{ section.settings.show_controls }}'
>
  {%- for block in section.blocks -%}
    {%- case block.type -%}
      {%- when 'banner_slide' -%}
        <div
          class='banner-slide{% if forloop.first %} active{% endif %}'
          {{ block.shopify_attributes }}
        >
          <!-- Imagen de fondo -->
          {%- if block.settings.background_image != blank -%}
            <div class='banner-background'>
              {{
                block.settings.background_image
                | image_url: width: 1920
                | image_tag: class: 'absolute inset-0 w-full h-full object-cover', loading: 'lazy'
              }}
            </div>
          {%- endif -%}

          <!-- Overlay de gradiente -->
          <div class='banner-overlay'></div>

          <!-- Contenido del banner -->
          <div class='page-width'>
            <div class='banner-content banner-content--{{ block.settings.text_alignment }}'>
              <div class='banner-text-container'>
                <!-- Título -->
                {%- if block.settings.title != blank -%}
                  <h2
                    class='banner-title'
                    style='color: {{ block.settings.title_color | default: section.settings.default_title_color }};'
                  >
                    {{ block.settings.title }}
                  </h2>
                {%- endif -%}

                <!-- Subtítulo con línea -->
                {%- if block.settings.subtitle != blank -%}
                  <div class='banner-subtitle-container'>
                    <p
                      class='banner-subtitle'
                      style='color: {{ block.settings.subtitle_color | default: section.settings.default_subtitle_color }};'
                    >
                      {{ block.settings.subtitle }}
                    </p>
                  </div>
                {%- endif -%}

                <!-- Botón -->
                {%- if block.settings.button_text != blank and block.settings.button_url != blank -%}
                  <a
                    href='{{ block.settings.button_url }}'
                    class='banner-button banner-button--{{ block.settings.button_style }}'
                    style='background-color: {{ block.settings.button_color | default: section.settings.default_button_color }}; color: {{ block.settings.button_text_color | default: section.settings.default_button_text_color }}; border-color: {{ block.settings.button_color | default: section.settings.default_button_color }};'
                  >
                    {{ block.settings.button_text }}
                  </a>
                {%- endif -%}
              </div>
            </div>
          </div>

          <!-- Imagen principal -->
          {%- if block.settings.main_image != blank -%}
            <div class='banner-main-image'>
              {{
                block.settings.main_image
                | image_url: width: 800
                | image_tag: class: 'banner-main-img', loading: 'lazy'
              }}
            </div>
          {%- endif -%}
        </div>
    {%- endcase -%}
  {%- endfor -%}

  <!-- Controles de navegación -->
  {%- if section.settings.show_controls -%}
    <div class='slider-controls-container'>
      <button class='slider-controls slider-controls--prev' aria-label='Banner anterior'>
        <svg width='24' height='24' viewBox='0 0 24 24' fill='none' xmlns='http://www.w3.org/2000/svg'>
          <path d='M15 18L9 12L15 6' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/>
        </svg>
      </button>
      <button class='slider-controls slider-controls--next' aria-label='Banner siguiente'>
        <svg width='24' height='24' viewBox='0 0 24 24' fill='none' xmlns='http://www.w3.org/2000/svg'>
          <path d='M9 18L15 12L9 6' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/>
        </svg>
      </button>
    </div>
  {%- endif -%}

  <!-- Indicadores -->
  {%- if section.blocks.size > 1 -%}
    <div class='slider-indicators'>
      {%- for block in section.blocks -%}
        {%- if block.type == 'banner_slide' -%}
          <button
            class='slider-indicator{% if forloop.first %} active{% endif %}'
            aria-label='Ir al banner {{ forloop.index }}'
            data-slide='{{ forloop.index0 }}'
          ></button>
        {%- endif -%}
      {%- endfor -%}
    </div>
  {%- endif -%}
</div>

{{ 'components/banners-container-slider.css' | asset_url | stylesheet_tag }}

{% schema %}
{
  "name": "Banner Rotador",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "Configuración de Rotación"
    },
    {
      "type": "range",
      "id": "rotation_speed",
      "min": 3000,
      "max": 9000,
      "step": 1000,
      "unit": "ms",
      "label": "Velocidad de Rotación",
      "default": 5000
    },
    {
      "type": "checkbox",
      "id": "auto_play",
      "label": "Reproducción Automática",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_controls",
      "label": "Mostrar Controles de Navegación",
      "default": true
    },
    {
      "type": "header",
      "content": "Colores por Defecto"
    },
    {
      "type": "color",
      "id": "default_title_color",
      "label": "Color del Título",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "default_subtitle_color",
      "label": "Color del Subtítulo",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "default_button_color",
      "label": "Color del Botón",
      "default": "#dc2626"
    },
    {
      "type": "color",
      "id": "default_button_text_color",
      "label": "Color del Texto del Botón",
      "default": "#ffffff"
    }
  ],
  "blocks": [
    {
      "type": "banner_slide",
      "name": "Banner",
      "settings": [
        {
          "type": "header",
          "content": "Imágenes"
        },
        {
          "type": "image_picker",
          "id": "background_image",
          "label": "Imagen de Fondo"
        },
        {
          "type": "image_picker",
          "id": "main_image",
          "label": "Imagen Principal"
        },
        {
          "type": "header",
          "content": "Contenido"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Título",
          "default": "Tu título aquí"
        },
        {
          "type": "text",
          "id": "subtitle",
          "label": "Subtítulo",
          "default": "Tu subtítulo aquí"
        },
        {
          "type": "text",
          "id": "button_text",
          "label": "Texto del Botón",
          "default": "Ver Más"
        },
        {
          "type": "url",
          "id": "button_url",
          "label": "URL del Botón"
        },
        {
          "type": "header",
          "content": "Diseño"
        },
        {
          "type": "select",
          "id": "text_alignment",
          "label": "Alineación del Texto",
          "options": [
            {
              "value": "left",
              "label": "Izquierda"
            },
            {
              "value": "center",
              "label": "Centro"
            },
            {
              "value": "right",
              "label": "Derecha"
            }
          ],
          "default": "left"
        },
        {
          "type": "select",
          "id": "button_style",
          "label": "Estilo del Botón",
          "options": [
            {
              "value": "primary",
              "label": "Primario"
            },
            {
              "value": "secondary",
              "label": "Secundario"
            },
            {
              "value": "outline",
              "label": "Outline"
            }
          ],
          "default": "primary"
        },
        {
          "type": "header",
          "content": "Colores Personalizados"
        },
        {
          "type": "color",
          "id": "title_color",
          "label": "Color del Título Personalizado"
        },
        {
          "type": "color",
          "id": "subtitle_color",
          "label": "Color del Subtítulo Personalizado"
        },
        {
          "type": "color",
          "id": "button_color",
          "label": "Color del Botón Personalizado"
        },
        {
          "type": "color",
          "id": "button_text_color",
          "label": "Color del Texto del Botón Personalizado"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Banner Rotador",
      "blocks": [
        {
          "type": "banner_slide"
        },
        {
          "type": "banner_slide"
        }
      ]
    }
  ]
}
{% endschema %}
